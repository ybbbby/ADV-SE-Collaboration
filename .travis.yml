matrix:
  include:
    - language: python
      python: 3.7
      install:
        - cd backend/
        - pip install -r requirements.txt
      script:
        - python -m tests.test_all

    - language: node_js
      node_js:
        - 14
      before_script:
        - cd frontend
        - yarn install
      script:
        - yarn test -u

notifications:
  email:
    - dq2146@columbia.edu
